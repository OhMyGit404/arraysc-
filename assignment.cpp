#include "arrays.h"

int main(){

    int capacity = 3;
    int array[capacity];

    get_age(array,capacity);
    //show(array,capacity);
    sum(array,capacity);
    average(array,capacity);
    maximum(array,capacity);
    minimum(array,capacity);

}